# 
Make an app to health cancer patiens to teach in a calendar what medecine to take
we need to input syntoms  in a chat bot interpase


we need also a doctor interphase that based on some criteria, input some sintoms , generate the ordenance for each patient, so it means that each patient has each profile and the avility to see its medecine calendar

patient cant modify anythin, is the doctor that can set the orders to do, even manually set a schedule that was created before

Its like an acompany app that will lead the patient to keep 

patient can confirm when he took it’s medecine

patient can input new syntoms and get guidance based on them (chat bot plus rag with current medecine schedule)

the ordenance should be sync with creaded ordenance on paper

notification as reminder when you need your app

doctor generates a test calendar schedule and then can manually modify per patient or add stuff

dont add login or auth, create a manual access code so doctor creates a manual code and this will generate a login session in the auth app

patient should input the start of the quimio adn then this will be aligned with the claude \


patient input code - input chimio date / weight - see agenda


